export interface UserDetails {
  firstName: string;
  lastName: string;
  workday: string;
  title: string;
  project: string;
  date_hired: string;
}

export interface TableElement {
  name: string;
  workday: string;
  title: string;
  project: string;
  date: string;
}
